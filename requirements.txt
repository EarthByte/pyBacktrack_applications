# Pip requirements for pyBacktrack_applications.
#
# These are sufficient to reproduce every figure in the paper *except*
# the ones that require pygplates (Figs 2, 3).  pygplates is published
# on conda-forge only; see environment.yml for a one-step install that
# includes it.
#
# Suggested install:
#     pip install -r requirements.txt
#
# Or, for the full stack with pygplates + GMT:
#     conda env create -f environment.yml
#     conda activate pybacktrack-applications
#
pybacktrack>=1.5
pygmt>=0.12
gplately>=1.3
numpy>=1.24
xarray>=2023.1
matplotlib>=3.7
cartopy>=0.22
netCDF4>=1.6
scipy>=1.10
python-docx>=1.0
shapely>=2.0
